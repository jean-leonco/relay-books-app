const common = {
  SomethingWentWrong: 'Something went wrong.',
  TheDescriptionShouldNot: 'The description must be less than {{size}} characters.',
};
export default common;
export type Keys = keyof typeof common;
